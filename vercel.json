{
  "buildCommand": "cd .. && pnpm --filter @workspace/layer run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd .. && npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
